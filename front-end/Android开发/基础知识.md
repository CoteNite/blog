# 基础知识

## 生态与基本架构

Android是由Google基于Linux内核创建的系统，分为四大部分：

1. Linux Kernel/Linux内核层
2. Libraries/核心类库层
3. Application Framework/应用框架层
4. Application/应用层

Android运行时算在核心类库层中，Android使用Dalvik虚拟机让每一个Android应用都运行在独立的进程中，Dalvik虚拟机是Google专门为Android定制的，对于移动端（手机平板）做了da'li